<h1>Edit Comment</h1>

<%= form_with(model: [@comment.destination, @comment], local: true) do |form| %>
  <%= form.label :content %>
  <%= form.text_area :content %>
  <%= form.submit "Update Comment" %>
<% end %>